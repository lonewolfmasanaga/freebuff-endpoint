# Fresh-machine setup (no git history needed)
node_modules/
logs/
data/

# Local secrets — never ship
config.json
*.log
config.json*
*.user-backup

# Live catalog cache (regenerated from the installed CLI on boot)
catalog.cache.json
